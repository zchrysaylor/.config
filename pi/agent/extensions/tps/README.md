# TPS

A small Pi extension that shows token generation speed while the model responds.

It shows:

- live tokens-per-second in the status line
- estimated token counts while streaming
- a final tokens-per-second summary when the agent finishes

The tracker is enabled automatically when a session starts. No commands are needed.
