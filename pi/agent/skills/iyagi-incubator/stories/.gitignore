# Default output folder for study sheets generated by the iyagi-incubator skill.
# This .gitignore is the only tracked file here: it keeps the (otherwise empty)
# folder in the repo while ignoring every generated .typ/.pdf a user creates.
*
!.gitignore
